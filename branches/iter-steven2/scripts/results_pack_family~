#!/bin/tcsh

if ( $#argv != "1" ) then
  echo results_pack_family ERROR: Need to specify case family
  exit
endif

set FAMILY = $1

cd $DIVHOME/results/$FAMILY

foreach FILE (`ls *.dat *.log`)
  set CASE = `echo $FILE | awk -F. '{print $1"\n"}'`
  echo $CASE

  results_pack $CASE
end
