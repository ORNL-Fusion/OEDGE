#!/bin/tcsh

if ( $#argv != "1" ) then
  echo results_unpack_family ERROR: Need to specify case family
  exit
endif

set FAMILY = $1

cd $DIVHOME/results/$FAMILY

foreach FILE (`ls *.dat`)
  set CASE = `echo $FILE | awk -F. '{print $1"\n"}'`
  results_unpack $CASE
end
