c     -*-Fortran-*-
C                                                                       
      COMMON /COMMV/  CRTABS, CRTRCS, CRVABS, CRAVAV, CRXMIN, CTBS,     
     >        CICIZS, CIFIZS, CILIZS, CISIZS, CICABS, CIFABS, CIFRIN,   
     >        CILABS, CISABS, CICUTS, CICCOL, CICRXA, CIFRXA, CFLRIN,   
     >        CICRCS, CIFRCS, CILRCS, CISRCS, CISRXA, CITRXA, CITRIN,   
     >        CTEXS , CFLRXA, CKKMAX, CICRIN, CISRIN, CXXX  , CSSS  ,   
     >        CNNN  , CKKMIN, CNNNX , CLLL  , CLLLX , CFLREX, CNNNS ,   
     >        CLLLS , CNNNK , CNNNT , CNNNKT, CMMM  , CMMMX , CMMMS ,   
     >        CIKRIN, CKTRIN, CICRNO, CIKRNO, CKTRNO, CICRNJ, CVVXC ,   
     >        CVVZM , CVVXE , CVVXP , CVVXS , CICLOS, CIFLOS, CILLOS,   
     >        CISLOS, CSSSS , CVVRM , CVVSM , CVVKM , cnorgs, cnorgr,
     >        cnorgz, cirrno, cizrno, cisrno,
     >        cvvrefm,cvvnrfm,cvvfpref,cvvfpnrf,cieizs,citizs
      save /commv/
c
      REAL CRTABS(MAXIZS), CRTRCS(MAXIZS), CRVABS(MAXIZS), CTBS(MAXIZS),
     >  CRAVAV(MAXIZS), CRXMIN, CITRXA, CIFRIN, CITRIN, CISRIN, CICRIN, 
     >  CICIZS(MAXIZS), CIFIZS(MAXIZS), CILIZS(MAXIZS), CKKMAX, CICCOL, 
     >  CISIZS(MAXIZS), CICABS(MAXIZS), CIFABS(MAXIZS), CTEXS(10),      
     >  CILABS(MAXIZS), CISABS(MAXIZS), CICUTS(MAXIZS), CICRXA, CIFRXA, 
     >  CICRCS(MAXIZS), CIFRCS(MAXIZS), CILRCS(MAXIZS), CISRCS(MAXIZS), 
     >  CISRXA, CKKMIN, CXXX  (MAXIZS), CSSS  (MAXIZS), CNNN(-1:MAXIZS),
     >  CNNNX (MAXIZS), CLLL(-1:MAXIZS),CLLLX (MAXIZS), CNNNS (MAXIZS), 
     >  CLLLS (MAXIZS), CNNNK (MAXIZS), CNNNT (MAXIZS), CNNNKT(MAXIZS), 
     >  CMMM(-1:MAXIZS),CMMMX (MAXIZS), CMMMS (MAXIZS), CIKRIN, CKTRIN, 
     >  CICRNO, CIKRNO, CKTRNO, CICRNJ, CVVXC , CVVZM , CVVXE , CVVXP , 
     >  CVVXS, CICLOS(MAXIZS), CIFLOS(MAXIZS), CILLOS(MAXIZS),          
     >  CISLOS(MAXIZS), CSSSS(MAXIZS), CVVRM , cvvkm  , cvvsm ,
     >  cnorgs(maxizs) , cnorgr(maxizs) , cnorgz (maxizs),
     >  cirrno, cizrno, cisrno, cvvrefm,cvvnrfm,cvvfpref,cvvfpnrf,
     >  cieizs(0:maxizs),citizs(0:maxizs)

      LOGICAL CFLRXA,CFLRIN,CFLREX                                      
